# 이 파일은 예시입니다. 실제 Secret은 kubectl로 생성하거나 CI/CD 파이프라인에서 관리하세요.
# 
# Secret 생성 방법:
# kubectl create secret generic auth-secrets \
#   --from-literal=database-password='your-secure-password' \
#   --from-literal=jwt-secret='your-jwt-secret-key' \
#   --namespace=dash
#
# 또는 base64 인코딩된 값으로:
# echo -n 'your-secure-password' | base64
#
# 그리고 아래와 같이 Secret 매니페스트를 생성:
#
# apiVersion: v1
# kind: Secret
# metadata:
#   name: auth-secrets
#   namespace: dash
# type: Opaque
# data:
#   database-password: <base64-encoded-password>
#   jwt-secret: <base64-encoded-jwt-secret>
#
# 주의: 실제 운영 환경에서는 Secret을 Git에 커밋하지 마세요!

apiVersion: v1
kind: Secret
metadata:
  name: auth-secrets
  namespace: dash
type: Opaque
stringData:
  # 데이터베이스 비밀번호 (실제 값으로 변경 필요)
  database-password: "change-me"
  
  # JWT 시크릿 키 (실제 값으로 변경 필요)
  jwt-secret: "change-me"
  
  # 기타 민감한 정보
  # sms-api-key: "change-me"

